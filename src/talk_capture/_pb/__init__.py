"""Generated meeting.v1 stubs, shipped with the package.

Committed rather than generated at build time: the package is installed by
people on their own hosts, and a build-time hook would require protoc and
grpcio-tools on their machine whenever pip builds from the sdist. Shipping them
means `pip install` needs no toolchain, and the package carries the contract
version it was built against.

Regenerate with `libs/capture/scripts/regen_stubs.sh`; CI checks they match
`proto/meeting/v1/meeting.proto`.
"""
